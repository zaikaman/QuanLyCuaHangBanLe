@model IEnumerable<QuanLyCuaHangBanLe.Models.Product>

@{
    ViewData["Title"] = "Quản lý sản phẩm";
}

<style>
    .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

    .btn-primary {
        padding: 12px 24px;
        background: var(--color-primary);
        color: var(--text-inverse);
        border: none;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        cursor: pointer;
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .btn-primary:hover {
        background: var(--color-primary);
        transform: translateY(-1px);
        box-shadow: var(--shadow-md);
        color: var(--text-inverse);
    }

    .products-card {
        background: var(--card-bg);
        border-radius: 12px;
        box-shadow: var(--shadow-sm);
        overflow: hidden;
        border: 1px solid var(--border-primary);
    }

    .products-header {
        padding: 20px 25px;
        border-bottom: 1px solid var(--border-primary);
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .search-filter {
        display: flex;
        gap: 15px;
        align-items: center;
    }

    .search-input {
        padding: 10px 15px;
        border: 1px solid var(--input-border);
        border-radius: 8px;
        width: 300px;
        font-size: 0.9rem;
        background: var(--bg-tertiary);
        color: var(--text-primary);
    }

    .search-input:focus {
        outline: none;
        border-color: var(--color-primary);
        background: var(--input-bg);
        box-shadow: 0 0 0 3px rgba(var(--color-primary-rgb), 0.1);
    }

    .filter-select {
        padding: 10px 15px;
        border: 1px solid var(--input-border);
        border-radius: 8px;
        font-size: 0.9rem;
        cursor: pointer;
        background: var(--bg-tertiary);
        color: var(--text-primary);
    }

    .products-table {
        width: 100%;
        border-collapse: collapse;
    }

    .products-table thead {
        background: var(--bg-tertiary);
    }

    .products-table th {
        padding: 15px 20px;
        text-align: left;
        font-weight: 600;
        color: var(--text-secondary);
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        border-bottom: 1px solid var(--border-primary);
    }

    .products-table td {
        padding: 18px 20px;
        border-bottom: 1px solid var(--border-primary);
        color: var(--text-primary);
        font-size: 0.95rem;
    }

    .products-table tbody tr {
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .products-table tbody tr:hover {
        background: var(--bg-tertiary);
    }

    .product-name {
        font-weight: 600;
        color: var(--text-primary);
    }

    .product-barcode {
        color: var(--text-secondary);
        font-size: 0.85rem;
        font-family: 'Courier New', monospace;
    }

    .price-cell {
        font-weight: 600;
        color: var(--color-success);
    }

    .actions {
        display: flex;
        gap: 10px;
    }

    .btn-action {
        padding: 8px 16px;
        border-radius: 8px;
        border: none;
        font-size: 0.85rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s ease;
        text-decoration: none;
        display: inline-block;
    }

    .btn-edit {
        background: var(--color-info-bg) !important;
        color: #2c5282 !important;
        border: 2px solid transparent !important;
    }

    .btn-edit:hover {
        background: #ffffff !important;
        border: 2px solid #2c5282 !important;
        color: #2c5282 !important;
    }

    .btn-delete {
        background: var(--color-error-bg) !important;
        color: #c53030 !important;
        border: 2px solid transparent !important;
    }

    .btn-delete:hover {
        background: #ffffff !important;
        border: 2px solid #c53030 !important;
        color: #c53030 !important;
    }

    .empty-state {
        text-align: center;
        padding: 60px 20px;
        color: var(--text-tertiary);
    }

    .empty-state-icon {
        font-size: 4rem;
        margin-bottom: 20px;
    }

    .empty-state-title {
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 10px;
        color: var(--text-secondary);
    }

    /* Pagination Styles */
    .pagination-container {
        padding: 20px 25px;
        border-top: 1px solid var(--border-primary);
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .pagination-info {
        color: var(--text-secondary);
        font-size: 0.9rem;
    }

    .pagination {
        display: flex;
        gap: 8px;
        list-style: none;
        margin: 0;
        padding: 0;
    }

    .page-item {
        display: inline-block;
    }

    .page-link {
        padding: 8px 14px;
        border: 1px solid var(--border-secondary);
        border-radius: 6px;
        color: var(--text-primary);
        text-decoration: none;
        font-weight: 500;
        transition: all 0.2s ease;
        background: var(--bg-secondary);
        display: inline-block;
        min-width: 40px;
        text-align: center;
    }

    .page-link:hover {
        background: var(--bg-tertiary);
        border-color: var(--color-primary);
    }

    .page-item.active .page-link {
        background: var(--color-primary);
        color: var(--text-inverse);
        border-color: var(--color-primary);
    }

    .page-item.disabled .page-link {
        color: #cccccc;
        cursor: not-allowed;
        background: #fafafa;
    }

    .page-item.disabled .page-link:hover {
        background: #fafafa;
        border-color: #e5e5e5;
    }
</style>

<div class="page-header">
    <div>
        <h1>📦 Quản lý sản phẩm</h1>
        <p style="color: #718096; margin-top: 5px;">Quản lý thông tin sản phẩm trong cửa hàng</p>
    </div>
    <a asp-action="Create" class="btn-primary">
        <i class="fas fa-plus"></i>
        Thêm sản phẩm mới
    </a>
</div>

<div class="products-card">
    <div class="products-header">
        <form asp-action="Index" method="get" style="margin: 0; display: contents;">
            <div class="search-filter">
                <input type="text" name="searchTerm" class="search-input" placeholder="🔍 Tìm kiếm sản phẩm..." value="@ViewBag.SearchTerm" />
                <input type="hidden" name="page" value="1" />
            </div>
        </form>
        <div style="color: #718096; font-size: 0.9rem;">
            Tổng: <strong style="color: #2d3748;">@ViewBag.TotalItems</strong> sản phẩm
        </div>
    </div>

    @if (Model.Any())
    {
        <table class="products-table">
            <thead>
                <tr>
                    <th>Mã SP</th>
                    <th>Tên sản phẩm</th>
                    <th>Mã vạch</th>
                    <th>Giá bán</th>
                    <th>Đơn vị</th>
                    <th>Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var product in Model)
                {
                    <tr>
                        <td>@($"SP{product.ProductId:D3}")</td>
                        <td>
                            <div class="product-name">@product.ProductName</div>
                        </td>
                        <td>
                            <div class="product-barcode">@product.Barcode</div>
                        </td>
                        <td class="price-cell">@product.Price.ToString("N0")đ</td>
                        <td>@product.Unit</td>
                        <td>
                            <div class="actions">
                                <a asp-action="Edit" asp-route-id="@product.ProductId" class="btn-action btn-edit">✏️ Sửa</a>
                                <a asp-action="Delete" asp-route-id="@product.ProductId" 
                                   class="btn-action btn-delete">🗑️ Xóa</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="empty-state">
            <div class="empty-state-icon">📦</div>
            <div class="empty-state-title">Chưa có sản phẩm nào</div>
            <p>Hãy thêm sản phẩm đầu tiên cho cửa hàng của bạn</p>
        </div>
    }

    @if (ViewBag.TotalPages > 1)
    {
        <div class="pagination-container">
            <div class="pagination-info">
                Hiển thị @((ViewBag.CurrentPage - 1) * 10 + 1) - @(Math.Min(ViewBag.CurrentPage * 10, ViewBag.TotalItems)) trong tổng số @ViewBag.TotalItems sản phẩm
            </div>
            <ul class="pagination">
                <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="Index" asp-route-page="@(ViewBag.CurrentPage - 1)" asp-route-searchTerm="@ViewBag.SearchTerm">‹ Trước</a>
                </li>

                @for (int i = 1; i <= ViewBag.TotalPages; i++)
                {
                    <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-searchTerm="@ViewBag.SearchTerm">@i</a>
                    </li>
                }

                <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="Index" asp-route-page="@(ViewBag.CurrentPage + 1)" asp-route-searchTerm="@ViewBag.SearchTerm">Sau ›</a>
                </li>
            </ul>
        </div>
    }
</div>

<script>
    // Auto-submit search form khi gõ (với debounce)
    let searchTimeout;
    document.querySelector('.search-input').addEventListener('input', function(e) {
        clearTimeout(searchTimeout);
        searchTimeout = setTimeout(() => {
            e.target.closest('form').submit();
        }, 500); // Đợi 500ms sau khi người dùng ngừng gõ
    });
</script>
